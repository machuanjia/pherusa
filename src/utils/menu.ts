/** @format */

export const initMenu = () => {
    // @ts-ignore
    new LaiyePublic({
        mode: 'dev',
        ativeSrc: 'partners',
        navCurretElement: document.getElementById('header'),
    })
}
